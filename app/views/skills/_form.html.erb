<%= form_for(@skill) do |skill_form| %>
  <% if @skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill.errors.count, "error") %> prohibited this skills from being saved:</h2>

      <ul>
      <% @skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= skill_form.label :"наименование" %><br />
    <%= skill_form.text_field :fullname %>
  </div>
  
 <div class="actions">
    <%= skill_form.submit %>
 </div>
  
<% end %>
